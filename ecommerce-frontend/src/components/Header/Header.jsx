import React from 'react';
import './Header.css';

// Hero component for the main hero section in the Shop page.

export const Hero = () => {
    return (
        <div className="hero">
            
        </div>
    );
}

export default Hero;
